// 设置视图：读写 config/settings.toml 和 .env
// 字段：API Key（脱敏）、Vault 路径、子目录名、模型配置
export default function Settings() {
  return (
    <div className="view-settings">
      <p>设置视图（待实现）</p>
    </div>
  )
}
